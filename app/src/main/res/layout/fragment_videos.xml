<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="com.exact.xtra.util.LayoutManagers"/>
        <variable name="viewModel" type="com.exact.xtra.ui.videos.VideosViewModel" />
    </data>

    <LinearLayout
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/sortBar"
            layout="@layout/sort_bar"
            app:visible="@{viewModel.loadedInitial}"
            app:liveData="@{viewModel.sortText}"/>

        <include
            android:id="@+id/recyclerViewLayout"
            layout="@layout/common_recycler_view_layout"
            app:viewModel="@{(com.exact.xtra.ui.common.PagedListViewModel)viewModel}"
            app:layoutManager="@{LayoutManagers.linear(context)}"/>

    </LinearLayout>
</layout>
